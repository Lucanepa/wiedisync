# KSCW Project

## Infrastructure
All infrastructure details (IPs, URLs, ports, credentials, deploy commands) are in **INFRA.md**. Always consult it before making infrastructure-related changes or when you need connection details.

## Tech Stack
- Frontend: React 19 + TypeScript + Vite + TailwindCSS
- Backend: PocketBase (SQLite, REST API, Realtime, Auth)
- Hosting: Cloudflare Pages (frontend), Synology NAS + Cloudflare Tunnel (backend)
- Language: German UI (Swiss German context), code in English

## Key Patterns
- **Mobile-first**: All UI must be designed mobile-first — responsive layout, touch-friendly targets (min 44px), and tested on small screens before desktop
- **Dark mode contrast**: Always ensure text/bg contrast in both light and dark mode. Every input, select, textarea must have explicit `dark:bg-gray-700 dark:text-gray-100` (or equivalent). Never leave default browser colors — they break in dark mode.
- **Hallenplan virtual slots**: Games, trainings, and GCal hall events are converted to `HallSlot`-shaped objects at display time (via `_virtual` metadata field) and merged with real `hall_slots`. They're never stored in the DB. See `INFRA.md → Hallenplan Virtual Slots` for the full mapping table.
- PocketBase hooks use isolated scopes — shared code must use `require(__hooks + "/file.js")` with `module.exports`
- `pb_hooks/` is gitignored (contains API keys) — deployed separately via SSH/rsync
- `.env` is gitignored — Cloudflare Pages env vars handle production config

## PocketBase Admin API

- **Adding columns/fields and creating records**: Can be done automatically via the API (no confirmation needed)
- **Deleting columns, collections, or records**: Must be confirmed with the user first, but can be executed by the agent

## Branches
- `main` → production (`kscw.lucanepa.com`)
- `dev` → preview (`dev-kscw.lucanepa.com`)

## Session Workflow

1. **Start of every chat**: Read `CLAUDE.md` and `INFRA.md` to get full project context before doing anything.
2. **End of every chat**: After finishing changes, append a few context lines to the **Changelog** section below summarizing what was done (date, short description). If entries get stale or redundant, overwrite with newer comments — keep it concise.

## Changelog
<!-- Newest entries on top. Overwrite old entries when they become redundant. -->
- **2026-03-10** — Test coverage expansion and stabilization: added Vitest unit test harness (`test:unit`) with focused coverage for `src/utils/memberPositions.ts` (sport-specific options, coercion, normalization, i18n keys). Extended Playwright auth setup with scoped admin storage states (`vb_admin`, `bb_admin`) plus rate-limit retry logic, added scoped-admin E2E coverage, updated CI env wiring for new role accounts, and hardened brittle member/public tests (team-detail + signup/home assertions, mobile snapshot threshold). Final verification: `CI= npm test` passed with `218 passed / 24 skipped`, and `npm run test:unit` passed (`6/6`).
- **2026-03-10** — Implemented full multi-position support for `members.position` (array values) with sport-scoped option sets in roster/team editing. Updated shared position helpers for legacy string coercion + array normalization, read-only rendering in profile/player views, and team-member normalization guard to auto-correct invalid position arrays by sport. Verified UI on localhost for VB/BB option separation, multi-select persistence, team detail display, and mobile layout sanity.
- **2026-03-10** — Refreshed Playwright mobile screenshot baselines for member `home` and `games` pages (`home-mobile-mobile-linux.png`, `games-mobile-mobile-linux.png`) after intentional UI updates; `e2e/tests/member/mobile-ui.spec.ts` now passes again in mobile project with unchanged diff threshold (`maxDiffPixelRatio: 0.05`).
- **2026-03-10** — Added team-sport position normalization guard in `useTeamMembers`: if a member has an out-of-sport position, the app now auto-normalizes it to `other` and persists the fix, preventing invalid cross-sport values from lingering after imports/manual edits.
- **2026-03-10** — i18n parity check and fixes: added missing `de.absences.subtitle`, `de.calendar.viewList`, `de.calendar.subtitleList`, and `en.games.subtitle` keys so DE/EN locale namespaces stay aligned and Absences page subtitle no longer falls back.
- **2026-03-10** — Expanded `members.position` frontend typing to support basketball roles (`point_guard`, `shooting_guard`, `small_forward`, `power_forward`, `center`) alongside volleyball roles. Added shared position helpers and sport-aware option filtering so team roster position selects only show positions for that team’s sport (with safe legacy fallback). Updated teams/auth position labels to render localized basketball position names in profile/read-only views.
- **2026-03-10** — Added sport-scoped admin roles `vb_admin` and `bb_admin` in frontend auth/permissions. New `useAuth` helpers (`hasAdminAccessToSport`, `hasAdminAccessToTeam`) gate team visibility and admin actions by sport (teams, trainings, scorer, hallenplan, and volleyball-only admin pages). DB/sync tools remain superadmin-only; INFRA.md now documents the role hierarchy and scoped-admin behavior. Updated role typing and profile/status badge display for new roles.
- **2026-03-10** — Basketplan sync (Basketball): added `basketplan` to `games.source` select field, `bp_team_id` text field on `teams`. Created 19 BB teams + synced 196 games via `scripts/fetch-basketplan.ts`. PB hooks: `bp_sync.pb.js` + `bp_sync_lib.js` — daily cron at 06:05 UTC (after SV sync at 06:00), manual trigger `POST /api/bp-sync`. Uses public XML API (no auth needed). Rankings sync for all current-season leagues (24 leagues, ~258 entries in `sv_rankings` with `bp-` prefixed `sv_team_id`). INFRA.md updated with Basketplan API section.
- **2026-03-09** — Terminplanung (Game Scheduling): new module for coordinating game slots with opponent teams. 4 new PB collections (`game_scheduling_seasons`, `game_scheduling_slots`, `game_scheduling_opponents`, `game_scheduling_bookings`). PB hooks: `game_scheduling_api.pb.js` (7 API endpoints: register, slots, book-home, propose-away, generate-slots, confirm-away, block-slot) + `game_scheduling_lib.js` (conflict detection: same-day, 1-day gap, cross-team via member_teams, hall closures, double-booking). Admin UI: `/admin/terminplanung` (season config, Spielsamstage editor, team slot config, slot generation, Excel import) + `/admin/terminplanung/dashboard` (per-team bookings overview, away proposal review, Excel export). Public opponent UI: `/terminplanung` (registration with team selector) + `/terminplanung/:token` (slot picker for home games, 3-proposal form for away games). Email notifications on booking confirmation (opponent + admin). Excel import/export via `xlsx` library. Turnstile CAPTCHA placeholder (test-token bypass for dev). i18n namespace `gameScheduling` (de/en). 15 new files in `src/modules/gameScheduling/`. Hooks deployed to NAS.
- **2026-03-09** — In-app notification system: new `notifications` PB collection (per-user, with dedup). PB hooks (`notifications.pb.js` + `notifications_lib.js`) auto-create notifications on games/trainings/events create/update/delete, game results (status→completed), training cancellations. Daily cron (06:30 UTC) for upcoming activity reminders + deadline reminders + 60-day cleanup. Frontend: `useNotifications` hook (fetch + realtime + mark-as-read), `NotificationBell` (desktop sidebar rail), `NotificationPanel` (full list modal/sheet), red dot on mobile "More" tab, notification row in MoreSheet. HomePage "Neuigkeiten"/"News" section shows latest 3 notifications. i18n approach: hooks store keys + JSON data, frontend renders localized text. New `notifications` i18n namespace (de/en). `Notification` type added. INFRA.md updated. User activity logs also added in this session.
- **2026-03-09** — User activity logs: new `user_logs` PB collection for centralized audit trail. `logActivity()` fire-and-forget utility (`src/utils/logActivity.ts`). `useMutation` hook auto-logs all create/update/delete. Added explicit logging to 12 files with direct `pb.collection()` mutations (signup, profile, teams, trainings, hallenplan, scorer, admin panel). Migrated scorer page from `scorer_edit_log` to `user_logs`. `UserLog` type added. INFRA.md updated with full `user_logs` section.
- **2026-03-06** — Hall closures system: merged GCal closure events + Schulferien into `hall_closures` collection. Added `gcal` and `schulferien` source types. Seeded 552 Schulferien records (46 holidays × 12 halls, 2025/26–2029/30) from Stadt Zürich data. Updated `gcal_sync_lib.js` to auto-create `hall_closures` for KWI A/B/C when GCal event matches closure pattern. ClosureManager rewritten with grouped view (by reason+dates+source), "Alle Hallen" label, colored source badges, group edit/delete. Admins can modify/delete all closures. i18n keys added (de/en). Spielhalle slots, pre-game shortening, SlotEditor KWI A+B combo, SlotBlock click fix — all from earlier session. INFRA.md updated with Hall Closures section.
- **2026-03-05** — Role-based team visibility & filtering: `/teams` and `/teams/:teamSlug` now require login (AuthRoute). Regular users see only their own team(s); admins/vorstand/coaches see all. Added `memberTeamIds`, `memberTeamNames`, `canViewTeam()` to `useAuth`. Excluded Basketball/Other from TeamFilter and TeamsPage (`sport="volleyball"` filter). Auto-select user's team(s) on Trainings and Games pages. GameDetailModal: participation moved to top with simple Yes/No buttons, scorer names with contact info for coaches. Participation restricted to user's own teams (games + trainings). ParticipationSummary compact spacing fix. Assigned test_user to H2. Updated E2E tests. i18n keys added (de/en).
- **2026-03-04** — Full participation system: RSVP on all activities (games, trainings, events), inline `ParticipationSummary` counts (✓/?/✗), `ParticipationRosterModal` (full team roster with statuses, absence overlay, deadline banner, tournament spots counter). `ParticipationButton` i18n + deadline awareness + max_players cap. `respond_by` date field on trainings/games/events, `max_players` on events. Coach-set deadlines in TrainingForm, EventForm, GameDetailModal (inline edit). PB hook `participation_reminders.pb.js`: daily cron (07:00 UTC) + manual trigger, sends language-aware emails to non-responders 1 day before deadline. New `participation` i18n namespace (de/en). 6 new files, 17 modified files.
- **2026-03-03** — Comprehensive UI quality E2E tests: expanded from 32 to 195 tests across 6 categories (accessibility, readability, overflow, sizing, grid-layout, mobile-ui). Added `@axe-core/playwright` for WCAG scanning. Fixed a11y issues: `aria-label` on SwitchToggle, Modal close button, MonthGrid nav buttons, ScorerPage filters (`htmlFor`/`id`), AssignmentEditor selects. HomePage CompactGameRow: vertical scores with green/red winner/loser colors, bold for Wiedikon team. Fixed calendar tests (Liste→Woche), trainings flakiness (reload-on-failure), sidebar selector specificity. INFRA.md updated with full test file structure and categories.
- **2026-03-02** — E2E testing with Playwright: 32 tests across 4 projects (setup, chromium, mobile, unauthenticated). Covers login flow, public page smoke tests (7 routes), auth guards (6 protected routes), dark mode, team detail, profile page, admin pages. GitHub Actions CI (`.github/workflows/e2e.yml`) runs on push/PR to dev/main. 6 test accounts in PB (user/vorstand/admin/superuser/coach/unapproved). Added `approved` bool field to members collection + backfilled 136 members. Page Object Model for LoginPage. Rate-limit-aware test sequencing. INFRA.md updated with full E2E section.
- **2026-03-01** — Team roster improvements: Bulk season update (2024/25→2025/26). TeamDetail table: displayName from first_name+last_name (member.name is empty), sortable column headers, email/phone/birthdate columns (responsive), inline editing (number, position, role for admin), `w-fit` table. Team slug URLs (`/teams/H1` instead of PB IDs) across all routes. RosterEditor rewrite: per-row role toggle chips (multi-select), editable numbers, dark mode, search on first/last name, sorted by last name. Removed `assistant` role entirely (cleared H3 assignment, removed from code + INFRA.md + i18n). MemberRow exported `getMemberRole`/`roleColors`. Bulk-updated all 130 members emailVisibility=true.
- **2026-03-01** — Native DB admin panel: replaced PocketBase admin iframe at `/admin/database` with full-featured 3-tab interface (SQL Editor, Tables, PB Admin). New PB hook `sql_admin.pb.js` for raw SQL execution (`POST /api/admin/sql`) with superadmin auth. Frontend: SqlEditor (Ctrl+Enter, query history, DDL confirmation), TableBrowser (collection sidebar, pagination, filter, sort), SchemaViewer (colored type badges), RecordEditModal (dynamic CRUD for all PB field types), ResultsTable (shared scrollable table). i18n strings (de/en). Hook deployed to NAS.
- **2026-03-01** — Profile onboarding & per-user language: added `language` select field (`english`/`german`) to members. One-time non-dismissable onboarding modal (ProfileEditModal in `onboarding` mode) triggers when `!user.language` — all users see it on next login. Mandatory: first_name, last_name, email, language. Optional: phone, number, birthdate, photo. Clubdesk notice banner for pre-populated data. Language selector previews locale live. After save, `useAuth` syncs i18n from `user.language`. Language toggle hidden in Layout sidebar + MoreSheet when preference is set. Modal.tsx `hideClose` prop for non-dismissable modals. New `src/utils/languageMap.ts`. Updated INFRA.md. PB hooks email localization documented (separate deploy).
- **2026-03-01** — Security hardening: filter injection fix (EmbedGamesPage, ical_feed.pb.js), XSS prevention via `sanitizeUrl()` on `social_url`/`maps_url` hrefs, `isApproved` strict check (`=== true`), useRealtime subscription leak fix + stable deps, `fields` param on member queries (PII minimization), 5MB photo upload limit, Hallenplan realtime debounce (300ms), `public/_headers` security headers (Cloudflare Pages). PB rate limiting enabled: `CF-Connecting-IP` proxy header + `/api/ical` custom rule. Documented in INFRA.md.
- **2026-03-01** — Slot claim system: auto-release + first-come-first-served claim for freed hall slots. New `slot_claims` PB collection. When training is cancelled or team is away, their hall slot shows as green "FREI" on Hallenplan. Any coach can claim it via ClaimModal. ClaimDetailModal for viewing/releasing claims. PB hooks for claim validation (duplicate prevention, past-date guard), auto-revocation (on uncancellation/game postponement), and email notification to all coaches when a slot is freed. Updated SlotBlock visual states, VirtualSlotMeta with isFreed/isClaimed, mergeVirtualSlots pipeline. i18n strings (de/en).
- **2026-02-28** — Role system simplification: moved team leadership roles (coach, assistant, captain, team_responsible) from `member_teams.role` to multi-relation fields on `teams`. Renamed `members.role` default from `player` to `user`. Removed `role` field from `member_teams` (now pure membership junction). Rewrote `useAuth.tsx` coach detection to query `teams` fields. Updated RosterEditor with chip-based leadership assignment UI. Updated MemberRow, TrainingForm, StatusBadge, SignUpPage.
- **2026-02-28** — Schreibereinsätze integration: added `scorer_licence` to members, 6 relation fields to games (`scorer_member`, `taefeler_member`, `scorer_taefeler_member`, `*_duty_team`), `scorer_edit_log` collection. Migrated 34 member licences + 77 game assignments from standalone app. Rewrote scorer module: relation-based AssignmentEditor, combined mode (Schreiber/Schiri), team overview tab, past games section, iCal export, audit logging. Updated GameDetailModal.
- **2026-02-28** — Members/Teams schema expansion: added `birthdate`, `yob` to members; `team_picture`, `social_url`, `sponsors`, `sponsors_logos` to teams. Migrated 39 players (H3+D2) from legacy DB with photos. Updated TeamDetail (banner, social link, sponsors section), PlayerProfile (age display), TeamCard (background image). MinIO S3 storage setup documented in INFRA.md.
- **2026-02-28** — HomePage: new public landing page at `/` with club logo, upcoming events (left), next 5 games + latest 5 results (right). CalendarPage moved to `/calendar`. Nav updated with Home + Calendar as separate entries in sidebar and bottom tab bar.
- **2026-02-28** — DB relations cleanup: added `hall` relation (multi) to `hall_events`, `hall` to `events`, `team` to `sv_rankings`. Deleted unused `users` collection. Updated GCal sync hook to auto-resolve hall IDs from title/location. Backfilled all 123 existing hall_events. Updated INFRA.md with full schema diagram.
- **2026-02-28** — Hallenplan virtual slots: games (home+away), trainings, and GCal hall events as virtual slots. VirtualSlotDetailModal, iCal modal. GCal sync hooks.
- **2026-02-28** — Member signup & approval system: two-branch signup (email check → claim existing account via password reset OR new registration with team selection + approval). `approved` + `requested_team` fields on members. PendingPage, AuthRoute gates on approval, coach/admin approval UI in TeamDetail. PB hook `signup_check.pb.js` for email existence check.
- **2026-02-28** — Calendar: home/away colors, away hall sync, `away_hall_json`, icon toggles, Vite dev port 1234.
