# KSCW Project

## Infrastructure
All infrastructure details (IPs, URLs, ports, credentials, deploy commands) are in **INFRA.md**. Always consult it before making infrastructure-related changes or when you need connection details.

## Tech Stack
- Frontend: React 19 + TypeScript + Vite + TailwindCSS v4 + shadcn/ui
- UI Components: shadcn/ui primitives in `src/components/ui/` (lowercase), KSCW wrappers in `src/components/` (PascalCase)
- Backend: Directus (Supabase Postgres, REST API, Realtime, Auth) on Hetzner VPS
- Hosting: Cloudflare Pages (frontend), Hetzner VPS + Cloudflare Tunnel (backend)
- Language: German UI (Swiss German context), code in English

## Data Format: TOON

This project uses TOON (Token-Oriented Object Notation) for passing structured data to LLMs.
TOON combines YAML-style indentation with CSV-style tabular arrays.

### Syntax rules
- Scalar: `key: value`
- Primitive array: `tags[3]: a,b,c`
- Object array: `users[2]{id,name,role}:\n  1,Alice,admin\n  2,Bob,user`
- Nested objects use indentation (like YAML)
- Strings with commas/spaces must be quoted
- Use `encode(data)` from `@toon-format/toon` to convert JSON → TOON

### When to use it
- When passing uniform arrays of objects to LLM prompts (saves ~40% tokens vs JSON)
- NOT for deeply nested/non-uniform data (use JSON-compact there)

### Example
\`\`\`toon
context:
  task: portfolio analysis
positions[3]{ticker,shares,costBasis}:
  AAPL,100,150.25
  MSFT,50,280.00
  NVDA,75,420.50
\`\`\`

## Work Style

- **Maximize parallel subagents**: When a task involves multiple independent pieces of work (e.g. editing different files, researching separate topics, running independent checks), always dispatch them as parallel subagents rather than doing them sequentially. This applies to exploration, implementation, testing, and any other work that doesn't have sequential dependencies.

## NEVER Commit Plans or Specs
**Plan documents, spec documents, and design docs must NEVER be committed to git.** They often contain credentials, tokens, internal URLs, and API examples with real auth headers. Plans generated by AI tools (superpowers, GSD, etc.) go in `.planning/` (gitignored) or stay ephemeral. The `docs/superpowers/plans/` and `docs/superpowers/specs/` directories are gitignored. If you generate a plan or spec, write it to `.planning/` or keep it in conversation only.

## Key Patterns
- **NEVER mass-email real users for testing**: When testing email features (event invites, notifications, newsletters), ALWAYS use the `/events/test-email` endpoint or target a single test member (e.g. member ID 8). Never call the notify endpoint with `send_email: true` on an event that targets all roles or a large audience. Mass emails to 200+ club members cannot be undone and will confuse real users.
- **shadcn/ui**: UI components use shadcn/ui on Tailwind CSS v4. Load `/kscw-shadcn` skill for KSCW-specific conventions (brand theming, component wrappers, migration map). Load `/tailwind-v4-shadcn` for general TW v4 + shadcn patterns. Full migration spec: `docs/superpowers/specs/2026-03-15-shadcn-migration-design.md`
- **Mobile-first**: All UI must be designed mobile-first — responsive layout, touch-friendly targets (min 44px), and tested on small screens before desktop
- **Dark mode contrast**: Always ensure text/bg contrast in both light and dark mode. Use shadcn semantic tokens (`bg-background`, `text-foreground`, `bg-primary`) which auto-switch in dark mode. For non-semantic colors, add explicit `dark:` variants.
- **Native `<select>` dark mode**: Native `<select>` / `<option>` elements do NOT inherit dark mode styles from Tailwind. `<option>` elements always render with the `<select>`'s background color. **Every `<select>` MUST have an explicit `dark:bg-gray-800`** (or equivalent dark background class) — `bg-transparent` alone results in white dropdowns in dark mode. This applies everywhere: forms, filters, modals, settings. When adding or reviewing any `<select>`, always verify it has a dark background class.
- **Hallenplan virtual slots**: Games, trainings, and GCal hall events are converted to `HallSlot`-shaped objects at display time (via `_virtual` metadata field) and merged with real `hall_slots`. They're never stored in the DB. See `INFRA.md → Hallenplan Virtual Slots` for the full mapping table.
- **Data integrity guards**: Postgres triggers enforce validation (slot claims, shell invites, coach approval). Game sync skips records with no `away_team`. See `directus/scripts/001-postgres-triggers.sql`.
- `.env` is gitignored — Cloudflare Pages env vars handle production config
- **Error logging**: ALL errors (frontend + backend) are logged to persistent JSONL files and accessible via `GET /kscw/admin/error-logs`. Load `/kscw-error-logs` skill for full query reference. Frontend errors also go to Sentry (de.sentry.io, org "kscw"). Check error logs FIRST when debugging any issue.
- **Troubleshooting**: When you encounter and solve an error, document it in `INFRA.md → Troubleshooting & Gotchas`. Check that section FIRST before debugging — the fix may already be documented.
- **M2M junction objects in forms**: When loading Directus M2M data into form state, always extract the related item ID from the junction object. Never pass raw expanded junction objects to UI components that expect `string[]`. Pattern: `.map((j: any) => String(typeof j === 'object' ? (j.related_field?.id ?? j.related_field ?? j) : j))`.
- **Promise.all in auth/context loading**: If one query in a `Promise.all` fails, all queries fail. For independent context queries (teams, coaches, etc.), ensure each collection actually exists in Directus before querying. After M2M recreations, always verify collection names via `GET /relations`.

## Directus Admin

- **Admin UI**: `https://directus.kscw.ch/admin` (prod), `https://directus-dev.kscw.ch/admin` (dev)
- **Schema changes**: Make on dev Directus, then use `npm run schema:pull` / `npm run schema:push` to sync to prod. See `INFRA.md → Schema Sync`.
- **Extensions**: Custom endpoints in `directus/extensions/kscw-endpoints/`, hooks in `directus/extensions/kscw-hooks/`. Deploy by restarting the Directus container.
- **Postgres triggers**: Validation logic in `directus/scripts/001-postgres-triggers.sql`. Apply via `psql` on `coolify-db`.
- **Deleting collections or records**: Must be confirmed with the user first
- **M2M fields MUST be created via the Directus admin UI** — creating M2M relations via the REST API results in "The relationship hasn't been configured correctly" in the admin UI, even though the API works. Always: (1) nuke the junction table + PG table + field, (2) create the M2M field via Settings → Data Model → Add Field → Many to Many in the browser, (3) restore data via API after. **Note:** the UI auto-generates junction names (e.g. `teams_members_3`) — after creation, rename via SQL to a readable name and update Directus metadata. Check actual names via `/relations` API before referencing in code.
- **Junction table names (prod)**: `hall_slots_teams`, `teams_coaches` (coach), `teams_responsibles` (team_responsible), `teams_sponsors`, `events_teams`, `hall_events_halls` (check with `GET /relations` if unsure). Note: `captain` is M2O on `teams` (not a junction table).

## SSH to VPS (Directus host)

- **Use `ssh hetzner`** (alias in `~/.ssh/config`) to reach the Hetzner VPS
- Directus runs as Docker containers: `directus-kscw` (port 8055), `directus-kscw-dev` (port 8056)
- Restart: `ssh hetzner "sudo docker restart directus-kscw"`
- Logs: `ssh hetzner "sudo docker logs --tail 30 directus-kscw"`
- See `INFRA.md → Hetzner VPS Management` for more commands

## Domains & Hosting

- **`kscw.ch`** — currently ClubDesk (external). Will eventually be migrated to point at KSCW platform. **Do NOT change until explicitly confirmed.**
- **`wiedisync.kscw.ch`** — React app production, CF Pages project `wiedisync` (`prod` branch) → `directus.kscw.ch`
- **`wiedisync.pages.dev`** — React app dev/preview, CF Pages project `wiedisync` (`dev` branch) → `directus-dev.kscw.ch` (auto-detected via hostname in `src/lib/api.ts`)
- **`directus.kscw.ch`** — Directus API production (Docker container `directus-kscw` on Hetzner, not managed by Coolify)
- **`directus-dev.kscw.ch`** — Directus API dev (Docker container `directus-kscw-dev` on Hetzner, not managed by Coolify)
- **`kscw-website.pages.dev`** — Public club website (static HTML), CF Pages project `kscw-website`. **Deploy to dev/preview only** until further notice — do NOT push website changes to production.
- **`kscw-push.lucanepa.workers.dev`** — Web push CF Worker

See `INFRA.md → Domains & Hosting Overview` for full domain map, future migration plan, and hardcoded URL reference table.

## Branches & Dev-First Workflow

- `prod` → production (`wiedisync.kscw.ch`, API: `directus.kscw.ch`)
- `dev` → preview (`wiedisync.pages.dev`, API: `directus-dev.kscw.ch`)

**All changes go through `dev` first.** Never push directly to `prod`. When asked to push/deploy Wiedisync or the public website, always push to `dev` unless explicitly told to push to `prod`/production. Workflow:

1. Develop and commit on `dev` branch
2. Deploy frontend to dev (push `dev` → Cloudflare Pages preview auto-deploys)
3. Deploy Directus extensions to dev: `npm run ext:deploy:dev` (rsyncs `directus/extensions/` to VPS `/opt/directus-kscw-dev/extensions/` and restarts the container — **not auto-deployed by Coolify**; Directus runs as plain Docker containers, not Coolify applications)
4. Test on `wiedisync.pages.dev` against `directus-dev.kscw.ch`
5. Once confirmed working, merge `dev` → `prod` (with user approval)
6. Push `prod` to trigger production frontend build, then `npm run ext:deploy:prod` for the backend extensions

## Session Workflow

1. **Start of every chat**: Read `CLAUDE.md` and `INFRA.md` to get full project context before doing anything.
2. **End of every chat**: After finishing changes, append a few context lines to the **Changelog** section below summarizing what was done (date, short description). If entries get stale or redundant, overwrite with newer comments — keep it concise.
3. **Before finishing**: Ask the user "Should this commit be added to the changelog and version bumped?" If yes, update `CHANGELOG.md`, bump `package.json` version (semver: patch for fixes, minor for features, major for breaking), and update the version in `src/modules/changelog/ChangelogPage.tsx` (both the `APP_VERSION` constant and the `CHANGELOG` data array). The changelog is displayed in-app via Options → What's New.

## Changelog
<!-- Keep recent entries. Overwrite when stale. For full history see git log. -->
- **2026-04-19** — Broadcast Plan 02 (In-App Chat channel) shipped to dev + prod (v3.14.0): `channels.inApp` is now a real third channel on the broadcast endpoint, scoped to **events only**. When set, the endpoint find-or-creates a persistent `activity_chat` conversation (`conversations.type='activity_chat'` + new `activity_type`/`activity_id` columns + partial unique index `uq_conversations_one_per_activity`), upserts audience + sender into `conversation_members` (honors `communications_banned` = skip, `communications_team_chat_enabled` = archived-flag; sender force-unarchived), and inserts the broadcast as a text message via ItemsService so Directus realtime emits (no `firePushForMessage` double-push — broadcast's own `channels.push` remains the sole push path, linking to `/events/:id`). Subject is embedded as `**${subject}**\n\n${message}` — no schema change. Audit row: `channels_sent.in_app` + `delivery_results.in_app { sent, failed, conversation_id, message_id }`. Response: same shape under `delivery.in_app`. Migrations 015/016/017 (idempotent): schema CHECK + shape CHECK (team | DM pair | activity_chat), event-only CHECK, `uq_conversations_one_per_activity`, participations→conversation_members sync trigger (AFTER INSERT/UPDATE/DELETE, no-op when no conversation exists), event-delete cleanup trigger (FK CASCADE handles children). Frontend: `src/modules/broadcast/BroadcastDialog.tsx` gains inApp checkbox — **hidden for games/trainings**, disabled with "Bald verfügbar" tooltip when `VITE_FEATURE_MESSAGING` off / not on allowlist, enabled otherwise. Success toast shows an "Chat öffnen" action deep-linking to `/inbox/:conversation_id`. `types/index.ts`: `ConversationType` gains `'activity_chat'`; `Conversation`/`ConversationSummary` gain optional `activity_type` + `activity_id`. Backend `shapeConversationSummary` + `GET /messaging/conversations` SELECT plumb them. `ConversationList` renders activity_chat rows with a Calendar icon + "EVENT" badge (title already populated with event.title at creation). `POST /messaging/messages` accepts `conv.type='activity_chat'` — no opt-in gate beyond membership, no block check (not a DM). Backend-only behavior: inApp works regardless of `VITE_FEATURE_MESSAGING` — the flag is a UI gate only. Tests: 25/25 unit (5 new for `findOrCreateActivityConversation` covering find/create/23505-race/non-event rejection/fatal-error propagation, 1 payload test replaced). 23/23 integration harness (10 new: inApp happy path, training/game rejection, inbox surfacing, non-member reply 403, 3 trigger paths — confirmed upsert / declined archive / banned delete, event-delete cleanup, audit row). Applied to prod DB `postgres` (not `directus_kscw_dev` — memory gotcha held). Version bumped 3.13.0 → 3.14.0; changelogs updated.
- **2026-04-19** — Broadcast (Contact-All) v1 shipped to dev + prod (v3.13.0): coaches/TRs/sport-admins/vorstand/admins can broadcast to event/game/training participants from the detail modal. New endpoints `POST /kscw/activities/:type/:id/broadcast` (+ `/preview`, debounced 300ms) — selectable channels (email + push, in-app deferred to Phase B / 501), audience filter by participation status (confirmed/tentative/declined/waitlist/interested/invited), event-only `includeExternals` toggle, 2000-char message + email subject (3-200), per-recipient locale-picked email via `buildBroadcastEmail` (extends existing `buildEmailLayout`), push fan-out via existing `sendPushToMembers` worker. RBAC server-side: event → admin/vorstand/sport-admin (sport derived from event's first linked team via `events_teams`); game/training → admin/sport-admin/coach (`teams_coaches`)/team_responsible (`teams_responsibles`). Per-recipient try/catch — partial failures captured in `delivery_results` jsonb, broadcast still returns 200. Rate-limit: max 3/hour/activity AND ≥20 min between sends → 429 with `retryAfterSec`. New `broadcasts` audit table (sender, audience snapshot, channel results, message body) — nFADP-conform. Frontend: `src/modules/broadcast/` with `canBroadcast` predicate (15 unit tests), `useBroadcast` + `useBroadcastPreview` hooks (no React Query, matches messaging hook style), shared `Modal` wrapper (auto-switches to drawer <640px), shadcn `AlertDialog` confirm step. Wired into `EventDetailModal` / `GameDetailModal` / `TrainingDetailModal` (modal-based detail views — no standalone routes exist). i18n `broadcast` namespace in de/en/fr/gsw/it (~22 keys). 19 backend unit tests + 13 integration assertions on dev — all green. Test emails routed to `.kscw.test` reserved domain (no human inboxes touched). **Sub-design shipped:** generic `event_signups` table replaces `mixed_tournament_signups` (polymorphic via `event` FK + `form_slug` + `form_data` jsonb) — 8 prod / 5 dev rows migrated, kscw-website form repointed (preview branch), Turnstile guard hook + `/public/mixed-tournament/non-member-count` endpoint cut over. Old table left as fallback; drop deferred (needs explicit user approval per CLAUDE.md). **DB-name gotcha caught (and re-added to memory):** prod Directus uses DB `postgres`, dev uses `directus_kscw_dev` — accidentally applied A1+A2 to prod first; recovery path was idempotent (no harm). Phase B (in-app chat channel via `conversations.activity_chat` polymorphism) deferred — Plan written, blocked on broader messaging feature flag rollout.
- **2026-04-19** — v3.12.0: messaging v1 shipped to prod (silent) + security hardening. Plan 06 executed: privacy-policy §9 with `#nachrichten` anchor across 5 locales, schema:push dev→prod (hit + fixed a `conversation_members` integer-id skeleton drift — had to drop + recreate to get uuid PK), 4 SQL migrations (007/008/009/010) applied to prod, 32 team conversations + 661 memberships backfilled, 6 triggers + 2 CHECK constraints + 11 missing indexes to match dev, RBAC via `009-messaging-permissions.mjs` + row-filters for deferred task #47 on blocks/message_requests/conversation_members (`$CURRENT_USER`-scoped), extensions deployed to prod, dev→prod merge `4927d07` + security-lockdown merge `6946b4c` pushed. **CF Pages prod flag flip still pending.** New feature: staged rollout via `VITE_FEATURE_MESSAGING_ALLOWLIST=id1,id2,id3` so a test group sees messaging before the club-wide flip. Security: new migration `011-revoke-supabase-anon-all.sql` revokes ALL from Supabase anon + authenticated roles on all public tables (43 tables previously had CRUD+TRUNCATE grants); Supabase API layer containers stopped (Kong/REST/Auth/Edge/Storage/MinIO/Studio/Meta/Vector/Analytics/Supavisor/imgproxy/realtime — all `--restart=no`) — only `supabase-db` still running. `tunnel:supabase.kscw.ch` + `tunnel:supabase-api.kscw.ch` now 502 by design. Hygiene: 2 test directus_users + 3 test members purged from dev (cascade clean), unused `yob` column dropped from members on both DBs, Federico's lingering static token rotated, 4 redundant Administrator `events` CRUD perms added back to both envs for strict parity (prod was originally canonical — feedback saved as memory). Full dev/prod permission parity verified: 9 policies × matching perm counts.
- **2026-04-18** — Messaging retention + export + push + consent + settings page (plan 05/6, dev-only, behind VITE_FEATURE_MESSAGING): 3 stub endpoints went live — `POST /settings/consent` (accepted/declined/later with flag side-effects), `POST /conversations/:id/clear` (self-purge soft-delete), `POST /export` (nFADP Art. 8 JSON bundle, 1/day rate-limited via new `members.last_export_at` column + `010-messaging-last-export.sql` migration). `POST /messages` + `POST /messaging/polls` gained non-blocking async push fan-out via the existing `sendPushToMembers` CF worker — muted + archived + cross-direction blocks excluded from recipient set; `push_preview_content` per member controls generic vs sender-name+truncated-body preview. New `schedule('0 3 * * *', …)` cron in `kscw-hooks` hard-deletes messages >12mo, soft-deleted messages >30d, declined requests >90d (FK cascade handles reactions). Frontend: `useMessagingConsent` hook + pure `resolveConsentState` helper (7-branch test) + `ConsentModal` three-button dialog (Ja/Nein/Später, non-dismissable, mounted in Layout) + `MessagingSettingsPage` at `/options/messaging` (consent status + 3 toggles + export button + info) + `ExportDataButton` (Blob download). `MessagingSettingsCard` reduced to a link; `SidebarOptions` gained a "Nachrichten" NavLink. ~20 new i18n keys × 5 locales. Harness extended with 6 Plan 05 assertions; **all stubs cleared — `EXPECTED_ENDPOINTS` is empty**. **Plan 06 next:** Vorstand sign-off + prod deploy + `VITE_FEATURE_MESSAGING=true` flag flip for all ~200 members.
- **2026-04-18** — Messaging reactions + edit/delete + reports + polls-in-messages (plan 04/6, dev-only, behind VITE_FEATURE_MESSAGING): 6 new endpoints live at `/kscw/messaging/*` — `POST /messages/:id/reactions` (toggle), `PATCH /messages/:id` (own-edit), `DELETE /messages/:id` (self + coach/TR team-moderator), `POST /reports`, `GET /reports` (admin), `PATCH /reports/:id` (admin resolve/dismiss + optional delete_message + optional ban) — plus one new endpoint `POST /messaging/polls` that creates a conversation-scoped poll + its `messages` row atomically. Helpers in `messaging-helpers.js`: `requireMessageOwner`, `requireTeamModerator`, `snapshotMessage`, `requireAdmin` (short-circuits for Directus system-admin tokens via `req.accountability.admin`). Moderator deletes auto-log a `reports` row with `reason='moderator_delete'` for audit. Ban via PATCH /reports/:id `{ ban:true }` flips `communications_banned=true` + both flags off. Admin notifications (`type='new_report'`) on each report via existing fan-out pattern (uses `date_created` DB default, not `created_at`). Frontend: `useReactions` + `useReports` + `useMemberDisplayNames` (pre-existing) + `useConversationPoll` hooks; `resolveMessageActions` pure helper under test (edit/delete/report action-set per caller+message); `ReactionBar` / `MessageActions` / `EditMessageInline` / `ReportMessageDialog` / `PollMessage` / `ComposerExtrasMenu` components; `MessageBubble` refactored with tombstone + edited indicator + actions/reactions integration. New `/admin/reports` page with Open/Resolved/Dismissed tabs + open-count badge + Resolve / Resolve+Delete / Ban / Dismiss actions. "Messaging blockiert" admin-only banner on PlayerProfile. Integration harness extended with 14 Plan 04 assertions. i18n messaging namespace +41 keys in de/en/fr/gsw/it + `moderationReports` nav key. No SQL migrations — Plan 01 pre-provisioned `message_reactions`, `reports`, `polls.conversation`, `chk_polls_team_or_conversation`. Retention cron / nFADP export / push fan-out / consent modal still 501-stubs — **Plan 05 closes them** before Plan 06 prod flip.
- **2026-04-18** — Messaging DMs + requests + blocks (plan 03/6, dev-only, behind VITE_FEATURE_MESSAGING): 5 new endpoints live at `/kscw/messaging/*` — `POST /conversations/dm`, `POST /requests/:id/accept`, `POST /requests/:id/decline`, `POST /blocks`, `DELETE /blocks/:member` — plus three extended endpoints (`GET /conversations` includes DMs + filters block-invisible pairs + populates `request_status` + `other_member`; `POST /messages` handles DMs/dm_requests with `dm_enabled` opt-in + bidirectional block check; `GET /conversations/:id/messages` filters blocked senders server-side). `PATCH /messaging/settings` live. Shared helpers in `messaging-helpers.js` (`requireDmEnabled`, `loadBlocks`, `shareTeam`, `findExistingDmConversation`, `checkDeclineCooldown`, `requireRequestRecipient`). New Postgres trigger `trg_messaging_dm_autoaccept` (migration `009-messaging-dm-autoaccept.sql`) auto-promotes pending requests to DM on shared-team insertion (block wins). Frontend `src/modules/messaging/` gained: `useBlocks` + `useMessageRequests` + `useMemberDisplayNames` hooks, pure `resolveRequestHeader` helper under test, `StartDmButton` / `BlockMemberDialog` / `RequestCard` / `ConversationList` / `InboxEmptyState` components, `ThreadView` extracted from `TeamMessagesTab` and reused, `InboxPage` at `/inbox`, `ConversationPage` at `/inbox/:id`, `MessagingSettingsCard` on `/profile` with team-chat + DM toggles. Nav: new **Inbox** sidebar item carries the unread badge; Teams item no longer decorated. `StartDmButton` entry point on `PlayerProfile`. i18n `messaging` + `nav` namespaces extended in de/en/fr/gsw/it. Integration harness extended with 12 Plan 03 assertions (soft-skip 2 for decline/cooldown when TOKEN_C absent) + idempotent `seed-plan03.mjs` + `trg_messaging_dm_autoaccept` in trigger list. Rerun of `009-messaging-permissions.mjs` confirmed Member-role RBAC on all 7 messaging collections after an earlier drift. Vitest 89/89, tsc + vite build clean. Reactions / polls / reports / retention / push fan-out / consent modal / `/options/messaging` still 501-stubs — Plans 04/05 build on this. **Known follow-up (deferred):** Member-role read on `blocks` / `message_requests` / `conversation_members` is unfiltered at the RBAC layer; hooks filter at fetch + client side so UI is correct, but privacy-tightening row-filters should land before the Plan 06 prod flip.
- **2026-04-18** — Messaging team chat (plan 02/6, dev-only, behind VITE_FEATURE_MESSAGING): 4 real endpoints live at `/kscw/messaging/*` — `GET /conversations`, `POST /messages`, `POST /conversations/:id/read`, `POST /conversations/:id/mute` — plus `GET /conversations/:id/messages` for paged thread fetch (cursor via `before`, default limit 50, max 200). Shared auth/membership helpers in `directus/extensions/kscw-endpoints/src/messaging-helpers.js` (`requireAuth`, `requireMember` via `members.user` FK, `loadConversationMembership` with 403 `not_a_member` per spec §4, `requireTeamChatEnabled`, `shapeConversationSummary`, `MessagingError`/`sendError`). Message writes go through `ItemsService.createOne` — raw knex inserts bypassed Directus's realtime emit pipeline, which broke subscribers. Frontend module `src/modules/messaging/` landed: `useConversations` / `useConversation` / `useUnreadTotal` hooks (all parameterized with optional `blockedSenderIds` for Plan 03), `MessageBubble` / `MessageComposer` / `ConversationThread` / `TeamMessagesTab` / `MessagingDisabledBanner` components, pure `shouldSubmitOnKeyDown` helper under test. Collapsible **Nachrichten** section on each team page (below polls) gated by `messagingFeatureEnabled()` (strict `=== 'true'` match). Unread badge on the Teams sidebar nav using semantic shadcn tokens (`bg-primary` / `text-primary-foreground`). i18n `messaging` namespace in de/en/fr/gsw/it (gsw uses de as stopgap). Also fixed Plan 01's double-`JSON.stringify` bug in `messagingApi` client (`kscwApi` stringifies internally; callers were pre-stringifying). Integration harness extended with 9 Plan 02 assertions (happy path + error codes + page fetch) + idempotent `seed-plan02.mjs`. Sentinel test user `plan02-a@kscw.ch` (directus_user linked to members.id=480). All 28 Plan 01 + 9 Plan 02 assertions green, 65/65 vitest, tsc + vite build clean. DMs, reactions, polls, reports, retention, push fan-out, consent modal still 501-stubs — Plans 03/04/05 build on this.
- **2026-04-17** — Messaging foundation (plan 01/6, internal-only): 7 new collections (conversations, conversation_members, messages, message_reactions, blocks, message_requests, reports), 7 new member fields (communications_*, last_online_at, consent_*), 2 poll fields (nullable team + conversation FK), 20 FK ON DELETE rules, 5 Postgres triggers (team-member sync, chat-toggle, team-create, sentinel-protect), sentinel system user (id=468), Directus RBAC for Member + Administrator, 501-stub endpoint skeleton at /kscw/messaging/*, TypeScript types, frontend messagingApi client + contract tests. No user-facing feature yet; plans 02-06 build on this foundation behind VITE_FEATURE_MESSAGING.
- **2026-04-17** — Vereinsnews hardening + schema fix (v3.11.2): **Root-cause fix for admin 403 on `/admin/announcements`**: `005-add-announcements.mjs` never created the standard Directus `date_created` / `date_updated` system fields (every other KSCW collection has them), so sorting the admin list by `-date_created` returned 403 *"field does not exist"*. Migration script now adds both with `date-created` / `date-updated` specials; re-ran against dev + prod. Admin list sort tiebreaker also switched to `-id` as a belt-and-suspenders workaround. **Security audit (S1+S2+S3)**: (S1) new `isSafeAppLink` in `src/utils/sanitizeUrl.ts` validates CTA links — admins posting `javascript:…` or `data:…` links are rejected on save (`AnnouncementsPage.handleSubmit`) and silently hidden on render (`AnnouncementDetailModal`). Prevents admin-XSS across all readers. (S2) Dropped `audience_teams` / `audience_roles` from the Member + Team Responsible read-permission field whitelists (via SQL `UPDATE directus_permissions` on both DBs + mirrored in `setup-permissions.mjs`) — v2 role/team targeting arrays would have leaked admin intent to all members. (S3) `notify_email=true` + `audience_type='all'` now triggers a `window.confirm` before saving, matching the CLAUDE.md mass-email guideline. F2 sport-audience bypass remains documented as accepted.
- **2026-04-17** — Admin Daten-Explorer fixes (v3.11.1): **Teams section** for member details now shows teams where the member is a coach, team-responsible, or captain (previously only `member_teams` junction → "Teams 0" for coaches/TRs/captains without player rows). `useExplorerCache` now also fetches `teams_coaches` + `teams_responsibles` junctions and derives captain from `teams.captain` M2O; unions all into the Teams table with new **Beziehung** column (Spieler:in / Trainer / Team-Verantwortlich / Captain). Sport-scope member filter also considers coach/TR/captain teams. **Member detail capitalisation**: subtitle uses localized bucket name (`Mitglieder · #11` not `members · #11`), sex `m`/`f` → `Männlich`/`Weiblich`, role array capitalized (`User, Vorstand`), participation status uses `explorerStatus_*` keys. **Orphan participations**: rows referencing events/trainings/games outside the 90-day cache window render as italic-muted `Event #6 (entfernt)` with type prefix instead of bare `#6`. **Referee-expenses 403**: root cause was `useRelatedEntities` filtering on non-existent fields (`referee`, `date`, `status`); fixed to use real schema (`paid_by_member`, `date_created`, `amount`, `notes`). Table now shows Datum / Betrag (CHF x.xx) / Notizen.
- **2026-04-17** — Vereinsnews (v3.11.0): new admin page `/admin/announcements` for posting club-wide announcements that surface in the homepage News card alongside notifications. Each post supports a hero image, per-locale title + rich-text body (de/en/fr/gsw/it via TipTap WYSIWYG editor stored as JSONB `translations`), optional CTA link, pin-to-top, expiry date, and audience targeting (`audience_type` enum: all/sport in v1, teams/roles schema-ready). Per-post `notify_push` + `notify_email` toggles fan out exactly once via `fanout_sent_at` guard in `kscw-hooks` action hook (resolves audience to `wiedisync_active` member IDs, sends push via `sendPushToMembers` and email via `MailService` with locale-resolved branded template). Homepage `HomePage.tsx` News card merges announcements with notifications into a unified feed (pinned first, then date-desc), capped at 3 with "Alle anzeigen" → `/news` archive page (paginated, 20/page). New `useAnnouncements` hook fetches with audience filter (sport-matched via `primarySport`) + realtime; new `RichTextEditor` (TipTap + StarterKit + Link + Placeholder, output compatible with existing `RichText` DOMPurify whitelist). Migration `005-add-announcements.mjs` is idempotent. RBAC: members get filtered read (`published_at._lte $NOW` AND non-expired); team responsibles + vorstand get full read; sport admins + admins get CRUD.
- **2026-04-17** — Admin Daten-Explorer (v3.10.0): new `/admin/explore` route — hierarchical read-only browser over 5 core entities (Mitglieder, Teams, Events, Trainings, Spiele). Single page-load batched cache (~200 KB / 5 parallel `fetchAllItems`), client-side fuzzy ranker (`rankEntities` in `useExplorerSearch`), URL deep-link (`?t=&id=`) + in-memory 10-entry breadcrumb nav stack in `ExplorePage`, lazy sub-sections via `useRelatedEntities` for participations/absences/schreibereinsätze/referee-expenses/scorer-delegations, sport-admin scoping via `getExplorerScope` (vb_admin/bb_admin only → single sport; club-wide events still visible). `buildFilters` scopes teams/trainings/games through `kscw_team.sport` on games (not `games.sport`). Mobile master-detail + ⌘K global search + Refresh button. Full i18n (de/en/fr/gsw/it) in existing `admin.ts` + `nav.ts` namespaces. Read-only; edits via "In Directus öffnen" link. 12 atomic commits in `src/modules/admin/{ExplorePage.tsx,components/,hooks/}`.
- **2026-04-17** — Team season format normalised + auto-rolling (v3.9.4): BB teams had long-form `2025/2026` while VB had short `2025/26` (+1 `2025/27` typo). Normalised 19 rows to `2025/26` on dev+prod. Converted `teams.season` Directus field to enum dropdown (`allowOther:false`) + Postgres CHECK `teams_season_format_check` (`^\d{4}/\d{2}$`). New yearly cron hook `schedule('0 3 1 5 *')` (May 1 03:00 UTC) in `kscw-hooks` rewrites dropdown to a 5-season window starting from the currently-live season (May→Aug: starts this autumn's; else: starts last autumn's) — so from May 1 2026 on, `2025/26` is dropped and `2030/31` is added. `games.season`/`rankings.season` were already short via `normalizeSeason()` in sync jobs.
- **2026-04-17** — Team join-request notification hook (v3.9.3): New `action('team_requests.items.create')` hook in `kscw-hooks` sends email + in-app notification + push to coaches/TRs of the requested team. Previously only `/register` had inline notifications; claim-flow additional-team requests via `team_requests` were silent.
- **2026-04-17** — Case-insensitive email lookup fix (v3.9.2): `/check-email`, `/register`, `/set-password` now use `LOWER(email)` matching + `directus_users` fallback. Fixes mixed-case emails (17 rows each in members/users, 1 vm_email) bypassing duplicate detection on signup and blocking password resets. Frontend `SignUpPage` redirects to `/login` on `code:email_exists`. DB normalised on prod+dev.
- **2026-04-17** — Team page load flash fix (v3.9.1): Derived `isLoading` (key-comparison) in `useTeamMembers`, `useMultiTeamMembers`, `useTeamAbsences` — eliminates one-frame empty-roster paint when `teamId` transitions from undefined → defined. `RosterEditor` also gates on `!team` to cover its own async team fetch.
- **2026-04-14** — Coach/TR participation editing (v3.9.0): Coaches and team responsibles can change other members' participation status inline in the roster modal (trainings/games only). Pencil icon → native `<select>` dropdown. Also works on waitlisted members. Users can override their own absence-declined status.
- **2026-04-10** — Interactive guided tours (v3.8.0): 10 React Joyride v3 tours (trainings, games, events, absences, scorer, hallenplan) with custom KSCW-branded tooltip. Welcome modal for first-timers, role-based auto-offers, central Guide menu at `/guide`, per-page "?" buttons. 5 languages, 36 unit tests. `src/modules/guide/`.
- **2026-04-06** — Bugfix dashboard (v3.7.0): AI-powered bugfix workflow. Admin `/bugfixes` page: view errors, trigger Claude Code fixes via GH Actions, deploy to dev/prod. Public `/status` page. 6 Directus endpoints, 2 GH Actions workflows, transaction-based rate limiting, shell injection prevention, key-based PII scrubbing.
- **2026-04-05** — SV licence card + security hardening (v3.6.0): Profile shows licence badges (LAS/Foreigner/FdO), federation, activated/validated — all from `sv_vm_check`. Stopped syncing licence fields to members. `sv_vm_check` permissions restricted to 11 safe fields. Absence cards restyled.
- **2026-04-05** — Expanded VM sync + email claim (v3.5.0): 16 new `sv_vm_check` columns (birthday, nationality, LAS, federation, double licence). Syncs all non-deceased players. `vm_email` on members for auto-linking during registration.
- **2026-04-05** — Junction table PK fix + rename: Dropped orphaned tables fixing 10 Sentry issues. Renamed 6 junction tables to readable names. Updated all metadata, frontend, backend, permissions.
- **2026-03-31** — Error log annotations (v3.1.0): Errors can be annotated (solved/important/open). Solved hidden by default. 3 new endpoints.
- **2026-03-30** — Directus RBAC (v2.10.0): 7 roles, 322 permissions. Role-sync hook. Public access restricted. Setup script idempotent.
- **2026-03-29** — Directus migration complete (v2.7–2.9): Security hardening, branded email templates, web push, 9 Postgres triggers, 30+ endpoints, Sentry, `stringifyIds()` for integer FKs, `asObj<T>()`/`relId()` utilities, SSO config, hallenplan M2O fix, nested filter objects.

### Earlier milestones (see git log for details)
- **2026-03-26** — Team settings accordion, RSVP improvements, team photo zoom, referee expenses
- **2026-03-24** — OTP-based auth (v2.1.0), coach visibility fix, RSVP timestamps
- **2026-03-12** — Scoreboard tab, rankings W/L splits, hallenplan free-slot generalization, web push, email templates, scorer delegation, ID standardization
- **2026-03-10** — Google OAuth, sport-scoped roles, basketball sync, game scheduling
- **2026-03-06** — Hallenplan (virtual slots, closures, claims), participation RSVP, notifications
- **2026-03-01** — Core platform: teams/members, Schreibereinsätze, admin tools, E2E tests, security
